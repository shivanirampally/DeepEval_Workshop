{
  "server": {
    "base_url": "http://192.168.1.81:11434"
  },
  "models": {
    "generator_preferences": [
      "llama3:instruct",
      "qwen2.5-coder:14b",
      "sqlcoder:15b"
    ],
    "judge_preferences": [
      "gpt-oss:20b",
      "qwen2.5:14b"
    ],
    "excluded_patterns": [
      "qwen3:30b",
      "qwen3-coder:30b",
      "deepseek-r1"
    ],
    "generator_count": 3
  },
  "trajectory": {
    "threshold": 0.7,
    "metrics": [
      {
        "name": "task_completion",
        "purpose": "Did the multi-generator workflow complete the requested task?"
      },
      {
        "name": "step_efficiency",
        "purpose": "Did the workflow avoid unnecessary generation and selection steps?"
      },
      {
        "name": "plan_adherence",
        "purpose": "Did the workflow follow the configured multi-generator evaluation and selection path?"
      }
    ],
    "temperature": 0
  },
  "execution": {
    "request_timeout_seconds": 180,
    "retries": 1
  },
  "project_1_report_root": "../project_1_multi_generator/reports",
  "workflow": {
    "prompt_template": "Answer the question using only the source. Do not guess or add unsupported facts.\n\nSource: {source}\nQuestion: {question}",
    "selection_policy": "Run the same three generators used by Project 1, record every output, then select the generator preferred by Project 1 when that report is available. This validates whether the trajectory reaches the same model-selection outcome."
  }
}